#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 * _abs - compute absolute int value
 * int r - define an int r
 * Return: Always 0.
 */
int _abs(int r)
{
   if (r < 0) {
        return -r;
    } else {
        return r;
    }
}
